work:
  profile: developer
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defauleZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
